```shell
# set correct kubernetes context
helm upgrade --install hello-world hello-world --values hello-world/values-azure.yaml
############
# set correct kubernetes context
helm upgrade --install hello-world hello-world --values hello-world/values-aws.yaml
```